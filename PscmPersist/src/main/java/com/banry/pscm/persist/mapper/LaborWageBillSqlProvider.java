package com.banry.pscm.persist.mapper;

import com.banry.pscm.service.labour.LaborWageBill;
import org.apache.ibatis.jdbc.SQL;

import static org.apache.ibatis.jdbc.SqlBuilder.*;

public class LaborWageBillSqlProvider {

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table labor_wage_bill
     *
     * @mbggenerated
     */
    public String insertSelective(LaborWageBill record) {
        SQL sql = new SQL();
        sql.INSERT_INTO("labor_wage_bill");
        
        if (record.getWageBillId() != null) {
           sql.VALUES("wage_bill_id", "#{wageBillId,jdbcType=VARCHAR}");
        }
        
        if (record.getWageId() != null) {
           sql.VALUES("wage_id", "#{wageId,jdbcType=VARCHAR}");
        }
        
        if (record.getYearMonth() != null) {
           sql.VALUES("year_month", "#{yearMonth,jdbcType=DATE}");
        }
        
        if (record.getWageItem() != null) {
           sql.VALUES("wage_item", "#{wageItem,jdbcType=VARCHAR}");
        }
        
        if (record.getWageItemPlus() != null) {
           sql.VALUES("wage_item_plus", "#{wageItemPlus,jdbcType=INTEGER}");
        }
        
        return sql.toString();
    }

    /**
     * This method was generated by MyBatis Generator.
     * This method corresponds to the database table labor_wage_bill
     *
     * @mbggenerated
     */
    public String updateByPrimaryKeySelective(LaborWageBill record) {
        SQL sql = new SQL();
        sql.UPDATE("labor_wage_bill");
        
        if (record.getWageId() != null) {
           sql.SET("wage_id = #{wageId,jdbcType=VARCHAR}");
        }
        
        if (record.getYearMonth() != null) {
           sql.SET("year_month = #{yearMonth,jdbcType=DATE}");
        }
        
        if (record.getWageItem() != null) {
           sql.SET("wage_item = #{wageItem,jdbcType=VARCHAR}");
        }
        
        if (record.getWageItemPlus() != null) {
           sql.SET("wage_item_plus = #{wageItemPlus,jdbcType=INTEGER}");
        }

        sql.WHERE("wage_bill_id = #{wageBillId,jdbcType=VARCHAR}");
        
        return sql.toString();
    }
}